{
  "schema_version": 1,
  "experiment_identifier": "experiment_id",
  "producing_git_commit": "commit_sha",
  "full_commit_sha_pattern": "^[0-9a-f]{40}$",
  "baseline_reference": "baseline_experiment_id",
  "forbidden_aliases": [
    "exp_id",
    "base_commit",
    "commit",
    "frozen_commit"
  ],
  "decision": "Use explicit experiment_id and commit_sha in every formal JSON contract. baseline_experiment_id refers to an experiment, never a Git revision."
}
