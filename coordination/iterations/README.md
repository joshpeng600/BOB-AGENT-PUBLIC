# Iterations

Store one immutable, reviewable coordination record per iteration. Each JSON record must include the full producing commit SHA and references to the approved config, manifest, and metrics.
